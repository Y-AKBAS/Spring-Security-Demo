server:
  port: 8099

spring:
  application:
    name: security-app
  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:

application:
  secret: "Tell no one"

logging:
    level:
      org.springframework.security: TRACE